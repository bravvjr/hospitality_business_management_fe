# Copy to .env.staging for docker compose -f docker-compose.staging.yml

# Backend API base URL (no trailing slash). Must match CORS_ORIGINS on the API.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Host port for the web container (default 3000).
WEB_PORT=3000
